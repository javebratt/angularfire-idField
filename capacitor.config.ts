import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angularfire-id',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
